export const metadata = {
  title: "Ecosistema Notarial",
  description: "Landing publica y acceso institucional de Ecosistema Notarial.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
