import { ProcessPlaceholder } from "@/components/process/process-placeholder";

export default function LotsPage() {
  return <ProcessPlaceholder section="Lotes" title="Operación masiva sobre el flujo documental" copy="La navegación y el shell quedaron alineados para que el siguiente paquete conecte lotes con creación masiva de casos, control de errores y generación documental." />;
}
